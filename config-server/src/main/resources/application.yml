server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  servlet:
    multipart:
      max-file-size: 50MB
#  security:
#    oauth2:
#      resourceserver:
#        jwt:
#          issuer-uri: "http://localhost:9090/realms/social-media"
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
